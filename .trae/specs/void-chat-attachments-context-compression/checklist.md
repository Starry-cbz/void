- [ ] 拖拽工作区文件/文件夹到聊天输入框可加入 selections，支持多选与去重
- [ ] `SELECTIONS` 序列化支持新增 Terminal selection，且不破坏现有 File/Folder/CodeSelection
- [ ] 支持将编辑器选区以 CodeSelection 加入对话（命令可用、UI 可见）
- [ ] 支持将终端片段手动加入对话（优先结构化输出，回退 readTerminal）
- [ ] 终端命令失败时右上角出现快捷按钮，点击可将失败片段加入对话
- [ ] 支持生成并注入 compressedContext（summary），且不强制删除 UI 历史
- [ ] 关键交互具备基础测试或静态校验，且类型检查/关键测试子集可通过

